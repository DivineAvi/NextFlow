if (node.id === nodeId) {
        // Always create a new object for the node and its data
        return {
          ...node,
          data: {
            ...node.data,
            ...newData,
          },
        };
      }